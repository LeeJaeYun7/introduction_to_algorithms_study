

```
Q 3.1.1
- f(n), g(n) 중 시간 복잡도가 큰 것을 기준으로, 나머지 것은 종속되기 때문에 O(f(n)+g(n))으로 나타낼 수 있다.

Q 3.1.2
- (n+a)^b의 최대차항이 n^b이기 때문에 O(n^b)가 된다. 

Q 3.1.3
- O(n2)은 점근적 상한이기 때문에, 적어도 O(n2)이라는 말은 의미가 없다.

Q 3.1.4
- 2^n+1 = O(2^n)이다. 2^2n = O(4^n)이다.

Q 3.1.5
- 세타는 빅오(점근적 상한) 빅오메가(점근적 하한)의 의미를 동시에 지니고 있으므로, 맞다.

Q 3.1.6
- 3.1.5와 동일한 내용이다.

Q 3.1.7
- 점근적 하한과 점근적 상한에는 교집합이 존재하지 않는다

Q 3.1.8
- 오메가(g(n,m)) = 모든 n>=n0와 m>=m0에 대해 0<= cg(n,m) <= f(n,m)인 양의 상수 c, n0, m0이 존재한다.
  세타(g(n,m)) = 모든 n>=n0와 m>=m0에 대해 0<=c1g(n) <= f(n) <= c2g(n)인 양의 상수 c1, c2, n0이 존재한다.





```
