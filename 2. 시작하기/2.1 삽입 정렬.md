```
Q.2-1-1
- 원소가 41일 때, 31보다 값이 크므로 while문에 진입하지 않는다
- 원소가 59일 때, 41보다 값이 크므로 while문에 진입하지 않는다
- 원소가 26일 때, 59가 값이 더 크므로, 59가 오른쪽으로 한 칸 이동한다.
                  41이 값이 더 크므로, 41이 오른쪽으로 한 칸 이동한다.
                  31이 값이 더 크므로, 31이 오른쪽으로 한 칸 이동한다.
                  A[1]에 26이 삽입된다.
- 원소가 41일 때, 59가 값이 더 크므로, 59가 오른쪽으로 한 칸 이동한다.
                  41과 값이 같으므로, while문에 진입하지 않고, A[4]에 41이 삽입된다.
- 원소가 58일 때, 59가 값이 더 크므로, 59가 오른쪽으로 한 칸 이동한다.
                  41보다 값이 크므로,  while문에 진입하지 않고, A[5]에 58이 삽입된다.


Q.2-1-2
- for j 2 to A.length
  key = A[j]

  i = j-1
  while i>0 그리고 A[i] < key
       A[i+1] = A[i]
       i = i-1
  A[i+1] = key

Q.2-1-3
- A배열을 INSERTION-SORT에 따라서 정렬을 한다
  그 다음에 A배열을 순차 탐색하면서, v값보다 값이 작으면, 오른쪽으로 탐색을 하고,
  v값과 같으면, 값을 찾고, break하고, v값보다 크면, break한다.

- A배열을 INSERTION-SORT를 통해서 정렬하기 때문에, 루프 불변성을 만족한다.


Q.2-1-4



   

  

