

```

Summary

- 이 절에서는 좋은 해시 함수 설계를 살펴보고, 해시 함수를 생성하는 세 가지 방법을 제시한다.
-> 이 중 두 개는 나누기와 곱셈을 이용하는 휴리스틱인 반면, 세 번째 유니버설 해싱은 랜덤화된 방법을 사용해
   좋은 성능을 증명할 수 있다.


1) 무엇이 좋은 해시 함수를 만드는가?
-> 좋은 해시 함수는 다른 키가 해시된 위치와는 상관없이 각 키가 m개의 자리 중 하나에 같은 확률로 해시된다는 단순 균등 해싱의 가정을 만족한다.
-> 불행히도 키가 추출되는 확률 분포를 알기 힘들기 때문에 일반적으로 이 조건을 검사할 방법이 없다.
-> 게다가 키가 독립적으로 추출되지 않을 수도 있다.
-> 때때로 분포를 알 수 있는 경우도 있다. 예를 들어, 키가 0<=k <1의 범위에 독립적이고 균등하게 분포된 무작위 실수 k임을 안다면
   다음 해시 함수는 단순 균등 해싱의 조건을 만족한다.

h(k) = [km]

- 실제로는 잘 동작하는 해시 함수를 생성하기 위해 종종 휴리스틱을 이용한다
-> 키들의 분포에 관한 정성적 정보가 해시 함수를 설계하는데 유용한 경우도 있다.
-> 예를 들어, 키가 프로그램의 식별자를 나타내는 문자열인 컴파일러의 심볼 테이블을 고려해보자
-> pt나 pts처럼 강한 연관성이 있는 기호들은 종종 동일한 프로그램에서 나타난다.
-> 좋은 해시 함수는 이와 같은 유사한 것이 동일한 자리에 해시되는 확률을 최소화해야 한다.

- 좋은 방법은 자료에 존재하는 어떠한 패턴에도 독립적이도록 해시값을 계산한다
-> 예를 들어, "나누기 방법"은 키가 특정한 소수로 나눈 나머지를 해시값으로 사용한다.
-> 이 방법은 키들의 분포에 나타나는 어떠한 경향과도 관련되지 않도록, 소수를 선택했다고 가정하면 흔히 좋은 결과를 가져온다.

- 마지막으로, 해시 함수의 어떤 응용은 단순 균등 해싱에 의해 제공되는 것보다 더 강한 특성을 요구할 수도 있다.
-> 예를 들어, 어떤 의미로 "가까운"키들의 해시값은 멀리 떨어지길 원할 수도 있다.
-> 11.3.3절에서 설명한 유니버설 해싱은 흔히 이런 특성을 제공한다. 


Insight
- 단순 균등 해싱의 의미. 좋은 해시 함수를 만든다는 것의 의미

Application
- 해시 함수 만들어보기 


```
