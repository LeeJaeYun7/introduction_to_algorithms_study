

```
Q11.2.1
- n/m 이라고 할 수 있다.

Q11.2.2
- 5가 index 5에 삽입된다.
  28이 index 1에 삽입된다.
  19가 index 1에 삽입되므로, 체이닝에 의해 연결된다.
  15가 index 6에 삽입된다.
  20이 index 2에 삽입된다.
  33이 index 6에 삽입되므로, 체이닝에 의해 연결된다.
  12가 index 3에 삽입된다.
  17이 index 8에 삽입된다.
  10이 index 1에 삽입되므로, 체이닝에 의해 연결된다.


Q11.2-3
-성공적 검색 - 값이 정렬되어 있는 걸 알고, 체이닝이 양방향 연결 리스트로 연결되어 있다면, 내가 찾고자 하는 값을 기준으로,
               그 값이 맨 앞에 값과 가깝다면 앞에서부터 찾고,
               그 값이 맨 뒤에 값과 가깝다면 뒤에서부터 찾는 식으로 검색 시간을 줄일 수 있다
- 실패하는 검색 - 어차피 전체 연결 리스트를 다 탐색해야 한다
- 삽입 - 항상 정렬을 유지해야 하므로, 오히려 삽입하는데 시간이 더 걸리게 된다.
- 삭제 - 똑같다. 


Q11.2-4
-














```
